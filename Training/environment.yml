name: elm-training
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - pytorch-cuda=12.1
  - pip
  - pip:
    - transformers>=4.51.0
    - accelerate>=0.27.0
    - safetensors>=0.4.0
    - wandb
    - tqdm>=4.65.0
    - numpy>=1.24.0
    - pyyaml
